import { StyleSheet } from "react-native";
export const style = StyleSheet.create({
    paginationStyle: {
        position: 'absolute',
        top: 20,
        left: "45%"
      },
      paginationText: {
        fontSize: 19,
        color: "#1890ff"
      }
});