import { StyleSheet } from "react-native";
export const style = StyleSheet.create({
    imageField: {
        flexDirection: "row",
        justifyContent: "space-between"
    },
    IconLearningField: {
        marginBottom: 40
    }
});